const {Course} = require('./course.model');
const {Professor} = require('./professor.model');
const {User} = require('./user.model');
const {Video} = require('./video.model');

module.exports ={
    Course,Professor,User,Video
}